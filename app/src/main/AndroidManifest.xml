<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sshopappui" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon_2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_2_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".emptyOffers" >
        </activity>
        <activity
            android:name=".Cart"
            android:theme="@style/splash_screen." />
        <activity
            android:name=".Home"
            android:theme="@style/splash_screen." />
        <activity android:name=".Finish" />
        <activity
            android:name=".items"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".menu"
            android:theme="@style/splash_screen." />
        <activity
            android:name=".Enter_item"
            android:theme="@style/splash_screen." />
        <activity
            android:name=".orderComfirmation"
            android:theme="@style/splash_screen." />
        <activity
            android:name=".viewPager"
            android:theme="@style/splash_screen." />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/splash_screen." >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>